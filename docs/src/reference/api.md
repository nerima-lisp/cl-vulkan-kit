# API reference

## `library-version`

```lisp
(library-version)
```

Return this system's version string, kept in sync with `cl-vulkan-kit.asd`'s
`:version`. The only export today — this repository has no Vulkan binding
yet.
